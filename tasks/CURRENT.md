# CURRENT.md — Active Task

> Project complete. No active task.

---

## Status

**Menubar app is built and working.** Fn+scroll volume (CoreAudio three-tier
fallback + NSAppleScript), Fn+horizontal brightness (DisplayServices via
`dlopen`). Custom status item icon. Launch at Login via SMAppService toggle.
Packaging: `scripts/deploy.sh` generates `.app` bundle and LaunchAgent.

Next session: check `tasks/BACKLOG.md` for any remaining polish items.
